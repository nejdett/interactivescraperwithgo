# Database Configuration
DB_NAME=cti_db
DB_USER=cti_user
DB_PASSWORD=your_secure_password_here

# Session Configuration
SESSION_SECRET=your_random_secret_key_here_min_32_chars

# Application Configuration
DASHBOARD_PORT=8080
COLLECTION_INTERVAL=5m
LOG_LEVEL=info

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values with secure random strings
# 3. Never commit .env file to version control
#
# To generate secure random strings:
# - Linux/Mac: openssl rand -base64 32
# - PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
